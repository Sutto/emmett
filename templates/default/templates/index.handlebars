<!DOCTYPE html>
<html>
  {{>head}}
  <body>
    {{>navigation}}

    <div class='container'>

      <div id='content'>
        {{{content}}}
      </div>

      <section id="document-index">
        <h2>API Index</h2>
        <div id='inner-index'>
          <ul>
            {{#groups}}
              <li>
                <h3><a href="{{url}}">{{name}}</a></h4>
                <ul class="endpoints">{{>endpoints}}</ul>
              </li>
            {{/groups}}
          </ul>
        </div>
      </section>

      {{>footer}}
    </div>
  </body>
</html>
